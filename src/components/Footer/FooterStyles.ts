import styled from 'styled-components';

const Copyright = styled.p`
  color: ${({ theme }) => theme.colors.mediumGrey};
  font-size: 0.85rem;
  padding: 2rem 5rem;
  margin: 0;
`;

export { Copyright };
